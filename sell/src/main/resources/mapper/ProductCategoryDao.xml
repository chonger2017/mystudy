<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.mapper.ProductCategoryDao">

    <resultMap id="productCategory" type="com.imooc.dataobject.ProductCategory">
        <result column="category_id" jdbcType="INTEGER" property="categoryId"/>
        <result column="category_name" jdbcType="VARCHAR" property="categoryName"/>
        <result column="category_type" jdbcType="INTEGER" property="categoryType"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>
    <insert id="save" parameterType="com.imooc.dataobject.ProductCategory">
       INSERT INTO product_category
       (category_name, category_type, create_time, update_time) VALUE
       (#{productCategory.categoryName,jdbcType=VARCHAR},
       #{productCategory.categoryType,jdbcType=INTEGER},
       now(),
       now()
       )
    </insert>
    <update id="updateProductCategory" parameterType="com.imooc.dataobject.ProductCategory">
        UPDATE product_category
        <set>
            <if test="categoryName != null">
                category_name = #{categoryName, jdbcType=VARCHAR},
            </if>
            <if test="categoryType != null">
                category_type = #{categoryType, jdbcType=INTEGER},
            </if>
            update_time=now()
        </set>
        WHERE category_id=#{categoryId, jdbcType=INTEGER}
    </update>

    <select id="getProductCategoryById" resultMap="productCategory">
        SELECT
        category_id as categoryId,
        category_name as categoryName,
        category_type as categoryType,
        create_time as createTime,
        update_time as updateTime
        FROM product_category
        WHERE category_id = #{categoryId,jdbcType=INTEGER}
    </select>
    <select id="getProductCategorys" resultType="com.imooc.dataobject.ProductCategory">
        SELECT
        category_id as categoryId,
        category_name as categoryName,
        category_type as categoryType,
        create_time as createTime,
        update_time as updateTime
        FROM product_category
    </select>

</mapper>